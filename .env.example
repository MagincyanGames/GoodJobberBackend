# JWT Secret Key
# IMPORTANTE: Cambia esto en producción por un valor aleatorio y seguro
# Puedes generar uno con: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Cloudflare Account ID (para migraciones remotas)
# CLOUDFLARE_ACCOUNT_ID=your-account-id

# Cloudflare API Token (para migraciones remotas)
# CLOUDFLARE_API_TOKEN=your-api-token
